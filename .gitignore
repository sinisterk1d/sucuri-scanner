node_modules
vendor

# Playwright
playwright/.auth
playwright/.report
playwright/.results
test-results

.wp-env.override.json

# Agent working files: guidance, plans and notes. Never committed, and never
# shipped — anything at the repo root that is not listed in .gitattributes as
# export-ignore ends up in the wordpress.org build.
CLAUDE.md
PLAN.md
MEMORY.md

.phpunit.result.cache
.vscode/launch.json
.vscode/settings.json
.idea/

opencode.json
composer.phar
.DS_Store
tests/fixtures/.htaccess
